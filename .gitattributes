# ============================================================================
#  换行符与二进制类型
#  ---------------------------------------------------------------------------
#  *.bat 必须是 CRLF：
#    cmd.exe 对 LF 结尾的批处理文件在 goto / 标签 / 括号块上有已知的解析问题。
#    仓库里 autocrlf 关掉、或在 Linux 上检出时，默认写法会把 .bat 变成 LF。
#    这里显式钉死，免得「双击没反应」这类问题从版本库里长出来。
#
#  另外提醒：本项目的 .bat 内容含中文，文件必须是 GBK(936) 编码且不含 chcp。
#    编码不属于 git 的职责范围，改脚本时请自行确认别存成 UTF-8。
#    背景见 docs/REFACTOR.zh.md 第六节与 LOCAL_SETUP.zh.md。
# ============================================================================
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资源：不要做任何换行或编码转换
*.png binary
*.ico binary
*.jpg binary
*.gif binary
